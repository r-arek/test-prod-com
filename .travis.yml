dist: focal
addons:
  apt:
    sources:
      - sourceline: 'deb https://repo.mongodb.org/apt/ubuntu/dists $(lsb_release -cs)/mongodb-org/4.4 multiverse'
    packages:
      - mongodb
script: mongod --version
