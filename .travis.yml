dist: bionic
language: java
services:
  - couchdb
script:
  - sudo systemctl status couchdb
  - sudo journalctl -u couchdb
  - sleep 300
  - sudo systemctl status couchdb
  - sudo journalctl -u couchdb
