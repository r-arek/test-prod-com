dist: focal
addons:
  apt:
    sources:
      - sourceline: 'deb https://repo.mongodb.org/apt/ubuntu $(lsb_release -cs)/mongodb-org/4.4 multiverse'
    packages:
      - mongodb
before_install: wget -qO - https://www.mongodb.org/static/pgp/server-4.4.asc | sudo apt-key add -
script: mongod --version
